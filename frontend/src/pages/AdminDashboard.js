import React from 'react';
import Dashboard from './Dashboard';

export default function AdminDashboard() {
  // For now, reuse Dashboard (can be extended for admin-specific features)
  return <Dashboard />;
}
