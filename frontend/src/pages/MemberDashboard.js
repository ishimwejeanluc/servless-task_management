import React from 'react';
import Dashboard from './Dashboard';

export default function MemberDashboard() {
  // For now, reuse Dashboard (can be extended for member-specific features)
  return <Dashboard />;
}
